import React from 'react'

import PageLoader from '@/app/[locale]/(website)/_components/page-loader'

type Props = {}

const Loading = (props: Props) => {
  return <PageLoader />
}

export default Loading
